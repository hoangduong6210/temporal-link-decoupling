---
title: Project Status
status: evidence frozen; paper pending
last_updated: 2026-08-21
paper_source: false
---

# Project Status

## Current state

The Link Prediction project has an independent implementation, corpus registry,
result partition, wiki, and paper root. Current scientific execution is complete
under `LP-E-SCIENTIFIC-MATRIX-001`, `LP-REL-2026-A003-001` is the active frozen
evidence release. No manuscript is evidence-admitted; `paper/CURRENT` is
`UNRELEASED`. The original mixed tree is read-only migration input and cannot
support current claims.

## Verification state

The source identity, exact raw and processed corpus digests, deterministic
clean-clone acquisition, conservative dataset-rights policy, resolved
configuration and protocol checksums, hashed transitive dependency lock,
state-neutral warmup, deterministic accelerator record, finite-state guards,
per-dataset topology, attempt ledger, failure matrix, and aggregate
reconstruction are closed.

The complete current task matrix and its terminal Slurm accounting are bound to
`LP-JOB-SLURM-A003-FINAL-RECONCILE-R2`. Superseded, failed, and cancelled work
retains native scheduler state under `LP-E-SCHEDULER-HISTORY-001`; none of those
attempts can silently enter the selected aggregate.

## Paper state

The current tree retains a single Link Prediction conference manuscript under
`paper/conference/`. Its source, PDF, grayscale figures, and Overleaf archive
are checksum-closed, but its complete result set is not registered in the
current frozen release. It is therefore a preserved submission artifact, not
an admitted scientific snapshot.

Project-authored source is released under BSD-3-Clause within the path-level
scope documented by the licensing policy. Dataset bytes, frozen evidence,
conference artifacts, and third-party material remain outside that grant.

Mutable candidate and working-paper copies are absent. Future paper text must be
written from the canonical wiki and admitted through the numeric-evidence gate.

## Revision boundary

Any venue-specific revision must be derived from the wiki, then create a source
commit, paper-build job, numeric registry, and immutable snapshot. Existing
frozen scientific evidence must never be overwritten.
