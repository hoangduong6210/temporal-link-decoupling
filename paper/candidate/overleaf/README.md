# Overleaf package

Upload the contents of this directory to Overleaf and compile `main.tex` with
pdfLaTeX. The archive is self-contained: it includes the manuscript, the
publication figures, and the unmodified IEEE conference class.

The figures are generated from the frozen evidence release by the repository's
conference-asset builder. Do not replace them with files from a working or
historical paper directory.
