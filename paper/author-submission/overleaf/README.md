# Overleaf package

Set `main.tex` as the main document. The package is self-contained: it includes
the IEEE conference class and all three referenced figures. The figures are
grayscale exports with the original dimensions and labels preserved.

The included `IEEEtran.cls` retains its upstream notice and is distributed
under the LaTeX Project Public License, version 1.3.
